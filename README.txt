# Checkout the options:
↪ ./bin/set_game --h
Usage: ./bin/set_game [OPTIONS]...
    -j, --json [JSON_DECK]           JSON deck
    -h, --help                       This help screen


# Play a Random Game:
./bin/set_game

# Play a Game based on a static deck:
./bin/set_game -j ./data/sample_deck.json
